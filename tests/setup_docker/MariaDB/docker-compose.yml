# Use root/example as user/password credentials
version: '3.1'

services:

  localmaria:
    image: mariadb
    restart: always
    environment:
      MYSQL_ROOT_PASSWORD: P@55w0rd

  adminer:
    image: adminer
    restart: always
    ports:
      - 8080:8080